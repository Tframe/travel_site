<html>

<head>
        <meta charset="utf-8">
        <title>What's Your Six? | Cities</title>

                <link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
                <link rel="stylesheet" href="assets/css/create-profile.css">
 
</head>
<body>

<h3>Add new activity:</h3>
<form id="addActivity" action="/activity" method="post">
    Activity name:<br> <input type="text" name="name"><br>
    Phone number: <br> <input type="text" name="phone_number"><br>
    City: <br><select name='city_id'><br>
        {{#each cities}}
          <option value='{{id}}'>{{name}}</option>
        {{/each}}
    </select>
    <br>
	<input type="submit" value="Submit">
 </form>
<br/>

<form id='filter_activity' action='' method=''>
<!--- this form does not submit directly, the button handles all the interaction with backend -->
    <label> Filter by City </label>
    <select id='city_filter' name="city"> 
		{{#each cities}}
		<option value='{{id}}'>{{name}}</option>
		{{/each}}
    </select><br>
    <input type="button" value="Filter" onclick="filterActivity()">
</form>
<br/>

<h3>Current Activities:</h3>
<table id="tableFormat">
    <thead> 
        <th>Activity Name</th> 
        <th>Phone Number</th>
	<th>City</th>
	<th>Delete</th>
	<th>Update</th>
    </thead> 
    <tbody>
        {{#each activity}} 
        <tr> 
            <td>{{name}}</td> 
            <td>{{phone_number}}</td>
	    <td>{{cityName}}</td>
            <td><button onclick="deleteActivity({{id}})">Delete</button></td>
            <td><a href="/activity/{{id}}">Update</a></td>
        </tr> 
        {{/each}} 
    </tbody> 
</table>

                <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
                <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/js/bootstrap.bundle.min.js"></script>
                <script src="assets/js/app.js"></script>
                <script src="assets/js/create-profile.js"></script>
        </body>
 
</html>
