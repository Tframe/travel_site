<html>
<head>
	<meta charset="utf-8">
	<title>What's Your Six? | Airlines Prices</title>
	<link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
        <link rel="stylesheet" href="assets/css/create-profile.css">
	</head>
<body>
  
  <br/>
<h3> Associate Price(s) with an Airline </h3>
<form id="addairlineprices" action="/airlineprice" method="post">
	Airline: <select name='airline_id'>
        {{#each airlines}}
          <option value='{{id}}'>{{name}}</option>
        {{/each}}
    </select>
	<br>
    Starting City: <select name='starting_city_id'>
        {{#each cities}}
          <option value='{{id}}'>{{name}}</option>
        {{/each}}
    </select>
	<br>
	Ending City: <select name='ending_city_id'>
        {{#each cities}}
          <option value='{{id}}'>{{name}}</option>
        {{/each}}
    </select>
	<br>
	Flight date: <input type="date" name="flight_date"><br>
    Price: <input type="number" value="1000" min="0" step="0.01" name="price"><br>
    <input type="submit" value="Submit">
	</form>
<br/>

<h3>Current Prices:</h3>
<table id="tableFormat">
    <thead> 
        <th>Airline Name</th>
		<th>Staring City</th>
		<th>Ending City</th>
        	<th>Book Date</th>
		<th>Price</th>
		<th>Delete</th>
		<th>Update</th>
    </thead> 
    <tbody>
        {{#each airline_price}} 
        <tr> 
            <td>{{name}}</td>
			<td>{{start_city}}</td>
			<td>{{end_city}}</td>			
            <td>{{book_date}}</td>
			<td>{{price}}</td>
            <td><button onclick="deleteHotelPrice({{hotel_id}})">Delete</button></td>
            <td><a href="/hotel/{{id}}">Update</a></td>
        </tr> 
        {{/each}} 
    </tbody> 
</table> 

                <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
                <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/js/bootstrap.bundle.min.js"></script>
                <script src="assets/js/app.js"></script>
                <script src="assets/js/create-profile.js"></script>
        </body>

</html>
