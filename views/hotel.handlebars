<html>

<head>
	<meta charset="utf-8">
	<title>What's Your Six? | Cities</title>
                <link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
                <link rel="stylesheet" href="assets/css/create-profile.css">
</head>
<body>

<h3>Add new hotel:</h3>
<form id="addHotel" action="/hotel" method="post">
    Hotel name: <input type="text" name="name"><br>
    Phone number: <input type="text" name="phone_number"><br>
    City: <select name='city_id'>
        {{#each cities}}
          <option value='{{id}}'>{{name}}</option>
        {{/each}}
    </select>

	<input type="submit" value="Submit">
 </form>
<br/>

<form id='filter_hotel' action='' method=''>
<!--- this form does not submit directly, the button handles all the interaction with backend -->
    <label> Filter by City </label>
    <select id='city_filter' name="city"> 
		{{#each cities}}
		<option value='{{id}}'>{{name}}</option>
		{{/each}}
    </select><br>
    <input type="button" value="Filter" onclick="filterHotel()">
</form>

<h3>Current Hotels:</h3>
<table id="tableFormat">
    <thead> 
        <th>Hotel Name</th> 
        <th>Phone Number</th>
	<th>City</th> 
	<th>Delete</th>
	<th>Update</th>
    </thead> 
    <tbody>
        {{#each hotel}} 
        <tr> 
            <td>{{name}}</td> 
            <td>{{phone_number}}</td> 
	    <td>{{cityName}}</td>
            <td><button onclick="deleteHotel({{id}})">Delete</button></td>
	    <td><a href="/hotel/{{id}}">Update</a></td>
	</tr> 
        {{/each}} 
    </tbody> 
</table> 

                <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
                <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/js/bootstrap.bundle.min.js"></script>
                <script src="assets/js/app.js"></script>
                <script src="assets/js/create-profile.js"></script>
        </body>


</html>
